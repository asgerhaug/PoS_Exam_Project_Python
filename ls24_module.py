from typing import List

# we first write skeloton function
def total(xs: List[float]) -> float:
    """ Total retuns the sum of xs."""
    return 0.0 #wrong 

# think of example uses
 #total([1, 2, 3]) should return 6.0 - intended use
 #total([100]) should return 100 - must work for one number
 #total([]) should return 0.0 - must work for wrong use
 
 